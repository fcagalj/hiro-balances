
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // make sure the path to App component is correct

ReactDOM.createRoot(document.querySelector('#root')).render(<App />);
